Added the backend app which takes file uploads and processes resumes. <br>
To run the app, <br>
cd into the backend directory and run 'docker compose up --build' and the app will run on 'localhost:8000'.
